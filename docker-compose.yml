services:
  api:
    build: .
    container_name: flood-api
    ports:
      - "8001:8000"
    env_file:
      - .env
    environment:
      APP_ENV: "local"
      LLM_API_PREFIX: "/llm"
    restart: unless-stopped
